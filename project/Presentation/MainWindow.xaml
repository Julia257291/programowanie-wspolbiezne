<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Bilard Symulacja" Height="600" Width="800">

    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Background="#F0F0F0">
            <TextBlock Text="Liczba kul:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Text="{Binding BallsCount, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="0,0,10,0" VerticalAlignment="Center"/>

            <Button Content="Generuj i Start" 
                    Command="{Binding StartCommand}" 
                    Padding="10,5" Margin="0,0,5,0"/>

            <Button Content="Stop" 
                    Command="{Binding StopCommand}" 
                    Padding="10,5"/>
        </StackPanel>

        <Border BorderBrush="Black" BorderThickness="2" Margin="10">
            <ItemsControl ItemsSource="{Binding Balls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Background="White" ClipToBounds="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse 
                            Width="{Binding Radius}" 
                            Height="{Binding Radius}"
                            Fill="Red"
                            Stroke="Black"
                            StrokeThickness="1"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </DockPanel>
</Window>
